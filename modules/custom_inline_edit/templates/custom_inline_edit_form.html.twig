
<span class="inline-edit-wrapper">
  <span id="{{ entity_id }}_{{ field }}" class="editable-value edit_{{ field }}" data-id="{{ entity_id }}" data-field="{{ field }}">{{ output }}</span>
  <span class="edit-link">
    <ul>
      <li><a href="/inline-edit/modal-form/{{ entity_id }}/{{ field }}" title="{{ 'Edit Field'|t }}" name="start-edit" class="use-ajax"><i class="fa fa-pencil-square"></i></a></li>
    </ul>
  </span>
</span>

{{ attach_library('custom_inline_edit/inline_edit_form') }}

